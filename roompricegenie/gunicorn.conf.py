# Increase the timeout setting to 300 seconds (5 minutes)
timeout = 300

# Number of worker processes for handling requests
workers = 3

# Worker class
worker_class = "sync"

# Bind to a specific address and port
bind = "0.0.0.0:8000"

# Log level
loglevel = "info"
